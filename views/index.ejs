<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>AI Learning Assistant | Programming Hub</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      color: #2d3748;
      line-height: 1.6;
    }

    .container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 20px;
    }

    /* Header */
    header {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      position: sticky;
      top: 0;
      z-index: 1000;
      border-bottom: 1px solid rgba(255, 255, 255, 0.2);
    }

    nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1.25rem 0;
    }

    .logo {
      font-size: 1.5rem;
      font-weight: 700;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-decoration: none;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }

    .nav-links a {
      color: #4a5568;
      text-decoration: none;
      font-weight: 500;
      transition: color 0.3s ease;
      position: relative;
    }

    .nav-links a::after {
      content: '';
      position: absolute;
      bottom: -5px;
      left: 0;
      width: 0;
      height: 2px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      transition: width 0.3s ease;
    }

    .nav-links a:hover {
      color: #667eea;
    }

    .nav-links a:hover::after {
      width: 100%;
    }

    /* Hero Section */
    .hero {
      text-align: center;
      padding: 4rem 0 3rem;
      color: white;
    }

    .hero h1 {
      font-size: 3rem;
      font-weight: 700;
      margin-bottom: 1rem;
      text-shadow: 0 2px 20px rgba(0, 0, 0, 0.2);
    }

    .hero p {
      font-size: 1.25rem;
      opacity: 0.95;
      max-width: 600px;
      margin: 0 auto;
    }

    /* Main Content */
    main {
      padding-bottom: 4rem;
    }

    .course-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: 2rem;
      margin-top: 2rem;
    }

    .course-card {
      background: white;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      cursor: pointer;
      position: relative;
      overflow: hidden;
    }

    .course-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 4px;
      background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform 0.3s ease;
    }

    .course-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
    }

    .course-card:hover::before {
      transform: scaleX(1);
    }

    .course-image {
      width: 100%;
      height: 180px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .course-image img {
      max-width: 100%;
      max-height: 100%;
      object-fit: contain;
      filter: drop-shadow(0 4px 20px rgba(0, 0, 0, 0.2));
      transition: transform 0.3s ease;
    }

    .course-card:hover .course-image img {
      transform: scale(1.1);
    }

    .course-content {
      padding: 1.5rem;
    }

    .course-content h3 {
      font-size: 1.5rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: #2d3748;
    }

    .course-content p {
      color: #718096;
      margin-bottom: 1.25rem;
      font-size: 0.95rem;
    }

    .course-links {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .course-link {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.75rem 1rem;
      background: #f7fafc;
      border-radius: 8px;
      text-decoration: none;
      color: #4a5568;
      font-weight: 500;
      transition: all 0.3s ease;
      border: 2px solid transparent;
    }

    .course-link:hover {
      background: #edf2f7;
      border-color: #667eea;
      transform: translateX(5px);
    }

    .course-link::before {
      content: '→';
      font-size: 1.25rem;
      color: #667eea;
      font-weight: bold;
    }

    /* Chat Widget Styles */
    #chat-toggle {
      position: fixed;
      right: 20px;
      bottom: 20px;
      width: 60px;
      height: 60px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      border-radius: 50%;
      cursor: pointer;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 24px;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      z-index: 1000;
    }

    #chat-toggle:hover {
      transform: scale(1.1);
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
    }

    #ai-chat {
      position: fixed;
      right: 20px;
      bottom: 20px;
      width: 320px;
      max-width: calc(100% - 40px);
      background: #ffffff;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
      overflow: hidden;
      border: 1px solid #e2e8f0;
      transform: translateY(100px);
      opacity: 0;
      visibility: hidden;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      z-index: 999;
    }

    #ai-chat.open {
      transform: translateY(0);
      opacity: 1;
      visibility: visible;
    }

    .chat-header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 12px 14px;
      font-weight: 600;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .close-chat {
      background: none;
      border: none;
      color: white;
      font-size: 18px;
      cursor: pointer;
      padding: 4px;
      border-radius: 4px;
      transition: background 0.2s ease;
    }

    .close-chat:hover {
      background: rgba(255, 255, 255, 0.2);
    }

    #chat-log {
      height: 220px;
      overflow-y: auto;
      padding: 12px;
      font-size: 14px;
      background: #f8fafc;
    }

    #chat-form {
      display: flex;
      gap: 8px;
      padding: 10px;
      border-top: 1px solid #e2e8f0;
      background: #fff;
    }

    #chat-input {
      flex: 1;
      padding: 10px 12px;
      border: 1px solid #cbd5e0;
      border-radius: 8px;
      outline: none;
      transition: border-color 0.2s ease;
    }

    #chat-input:focus {
      border-color: #667eea;
    }

    #chat-form button {
      background: #667eea;
      color: white;
      border: none;
      padding: 10px 12px;
      border-radius: 8px;
      cursor: pointer;
      transition: background 0.2s ease;
    }

    #chat-form button:hover {
      background: #5a6fd8;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .hero h1 {
        font-size: 2rem;
      }

      .hero p {
        font-size: 1rem;
        padding: 0 1rem;
      }

      .course-grid {
        grid-template-columns: 1fr;
        gap: 1.5rem;
      }

      nav {
        flex-direction: column;
        gap: 1rem;
      }

      .nav-links {
        gap: 1rem;
        flex-wrap: wrap;
        justify-content: center;
      }

      #ai-chat {
        width: calc(100% - 40px);
        max-width: none;
      }
    }

    @media (max-width: 480px) {
      .hero {
        padding: 2rem 0;
      }

      .hero h1 {
        font-size: 1.75rem;
      }

      #chat-toggle {
        width: 50px;
        height: 50px;
        font-size: 20px;
      }
    }
  </style>
</head>

<body>
  <header>
    <nav class="container">
      <a href="#" class="logo">🧠 AI Learning Assistant</a>
      <ul class="nav-links">
        <li><a href="#">Home</a></li>
        <li><a href="#">Courses</a></li>
        <li><a href="#">Resources</a></li>
        <li><a href="#">Contact</a></li>
      </ul>
    </nav>
  </header>

  <main class="container">
    <section class="hero">
      <h1>Master Programming Languages</h1>
      <p>Learn C, C++, Java, Python, and JavaScript with our comprehensive courses and resources</p>
    </section>

    <div class="course-grid">
      <article class="course-card">
        <div class="course-image">
          <img src="https://upload.wikimedia.org/wikipedia/commons/1/19/C_Logo.png" alt="C Programming" />
        </div>
        <div class="course-content">
          <h3>C Programming</h3>
          <p>Master the fundamentals of C language and build strong programming foundations</p>
          <div class="course-links">
            <a href="https://www.youtube.com/watch?v=KJgsSFOSQv0" target="_blank" class="course-link">Watch YouTube Tutorial</a>
            <a href="https://drive.google.com/file/d/1Uek0YApKaB_jbWJUTuHpvZ5UD599di8Q/view?usp=drivesdk" target="_blank" class="course-link">Download Notes (PDF)</a>
          </div>
        </div>
      </article>

      <article class="course-card">
        <div class="course-image">
          <img src="https://upload.wikimedia.org/wikipedia/commons/1/18/ISO_C%2B%2B_Logo.svg" alt="C++ Programming" />
        </div>
        <div class="course-content">
          <h3>C++ Programming</h3>
          <p>Dive into object-oriented programming concepts with C++</p>
          <div class="course-links">
            <a href="https://www.youtube.com/watch?v=vLnPwxZdW4Y" target="_blank" class="course-link">Watch YouTube Tutorial</a>
            <a href="https://drive.google.com/file/d/1lIt9cQ9GO9hCWaYb0ysMQiZyQ0Lbd7bG/view?usp=drivesdk" target="_blank" class="course-link">Download Notes (PDF)</a>
          </div>
        </div>
      </article>

      <article class="course-card">
        <div class="course-image">
          <img src="https://upload.wikimedia.org/wikipedia/en/3/30/Java_programming_language_logo.svg" alt="Java Programming" />
        </div>
        <div class="course-content">
          <h3>Java Programming</h3>
          <p>Learn core and advanced Java concepts from basics to expert level</p>
          <div class="course-links">
            <a href="https://www.youtube.com/watch?v=grEKMHGYyns" target="_blank" class="course-link">Watch YouTube Tutorial</a>
            <a href="https://drive.google.com/file/d/1oiiys4KomWiZhEJN31809TYWqFnDWWkQ/view?usp=drivesdk" target="_blank" class="course-link">Download Notes (PDF)</a>
          </div>
        </div>
      </article>

      <article class="course-card">
        <div class="course-image">
          <img src="https://upload.wikimedia.org/wikipedia/commons/c/c3/Python-logo-notext.svg" alt="Python Programming" />
        </div>
        <div class="course-content">
          <h3>Python Programming</h3>
          <p>Python for AI, Data Science, and modern application development</p>
          <div class="course-links">
            <a href="https://www.youtube.com/watch?v=_uQrJ0TkZlc" target="_blank" class="course-link">Watch YouTube Tutorial</a>
            <a href="https://drive.google.com/file/d/1cUFdfg-MBe_DgSRhuYnmlJYYvAQtL5s_/view?usp=drivesdk" target="_blank" class="course-link">Download Notes (PDF)</a>
          </div>
        </div>
      </article>

      <article class="course-card">
        <div class="course-image">
          <img src="https://upload.wikimedia.org/wikipedia/commons/6/6a/JavaScript-logo.png" alt="JavaScript Programming" />
        </div>
        <div class="course-content">
          <h3>JavaScript Programming</h3>
          <p>Master web development with JavaScript and DOM manipulation</p>
          <div class="course-links">
            <a href="https://www.youtube.com/watch?v=W6NZfCO5SIk" target="_blank" class="course-link">Watch YouTube Tutorial</a>
            <a href="https://drive.google.com/file/d/11DE8y91sRrgNL0p8pqw4piE5FL6J1p8w/view?usp=drivesdk" target="_blank" class="course-link">Download Notes (PDF)</a>
          </div>
        </div>
      </article>
    </div>
  </main>
  
  <!-- Toggle Button for Chat -->
  <button id="chat-toggle">💬</button>

  <!-- AI Chat Widget -->
  <div id="ai-chat">
    <div class="chat-header">
      <span>⚡ Code Chat (Cerebras)</span>
      <button class="close-chat">✕</button>
    </div>
    <div id="chat-log"></div>
    <form id="chat-form">
      <input id="chat-input" type="text" placeholder="Ask any coding question..." />
      <button type="submit">Send</button>
    </form>
  </div>

  <script>
    const chatToggle = document.getElementById('chat-toggle');
    const aiChat = document.getElementById('ai-chat');
    const closeChat = document.querySelector('.close-chat');
    const log = document.getElementById('chat-log');
    const form = document.getElementById('chat-form');
    const input = document.getElementById('chat-input');

    // Toggle chat visibility
    chatToggle.addEventListener('click', () => {
      aiChat.classList.add('open');
      chatToggle.style.display = 'none';
      input.focus();
    });

    // Close chat and show toggle button
    closeChat.addEventListener('click', () => {
      aiChat.classList.remove('open');
      chatToggle.style.display = 'flex';
    });

    // Add message to chat log
    function addMsg(role, text) {
      const wrap = document.createElement('div');
      wrap.style.marginBottom = '10px';
      wrap.innerHTML = `<div style="font-weight:600;color:${role==='You'?'#2d3748':'#4c51bf'}">${role}</div>`+
        `<div style="white-space:pre-wrap; color:#2d3748; background:${role==='You'?'#edf2f7':'#ffffff'}; padding:8px 10px; border-radius:8px; border:1px solid #e2e8f0;">${text.replace(/</g,'&lt;')}</div>`;
      log.appendChild(wrap);
      log.scrollTop = log.scrollHeight;
    }

    // Handle chat form submission
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const q = input.value.trim();
      if (!q) return;
      
      addMsg('You', q);
      input.value = '';
      addMsg('Bot', 'Thinking...');
      
      try {
        const res = await fetch('/ai/chat', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ message: q })
        });
        const data = await res.json();
        // replace last 'Thinking...' message
        log.removeChild(log.lastChild);
        addMsg('Bot', data.reply || 'Sorry, no response');
      } catch (err) {
        log.removeChild(log.lastChild);
        addMsg('Bot', 'Error contacting AI service');
      }
    });

    // Close chat when clicking outside (optional)
    document.addEventListener('click', (e) => {
      if (aiChat.classList.contains('open') && 
          !aiChat.contains(e.target) && 
          e.target !== chatToggle) {
        aiChat.classList.remove('open');
        chatToggle.style.display = 'flex';
      }
    });
  </script>
</body>
</html>